<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 맵핑될 DAO 인터페이스의 Full name 을 줍니다. -->
<!-- <mapper namespace="user.db.UserDAO">

	id 속성은 DAO 의 메소드와 같아야 한다. 여기의 boardVO 는 mybatis-confg.xml 에 Alias 되어 있습니다.
	<select id="selectBoardList" resultType="userVO"> 
	
	</select>
	
	<insert id="insertUser">
	insert into movie_user
     values(#{username}, #{userid}, #{password}, #{email}, #{password_q},#{password_a},#{phone},#{phone_zip},
	 ,#{postcode}, #{address}, #{detailAddress}, #{birth},#{sex})	
	</insert>
	
	<select id="checkId">
	select id from movie_user where id = #{userid}     
	</select>
	

</mapper>
 -->